import React from 'react';
import {allMembers, setAllMembers} from "../../API/API";

const Favorite = (id) => {

    const onClickHandler = (params) => {
        allMembers.forEach()
    }

    return (
        <i onClick={onClickHandler} className='bi bi-heart' />
    );
};

export default Favorite;
